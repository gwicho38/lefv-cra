# Release 1.0.6

## Changes from 1.0.5

### Documentation

- **Enhanced LSH environment documentation**: Added detailed instructions on using `--env` flag to define named environments (dev, staging, prod, etc.)
- **Added `lsh env` command**: CLI output now shows how to list available environments
- **Clarified environment naming**: Documented that environment names can be any string for flexible deployment configurations
